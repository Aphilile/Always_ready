def largest_number(numbers_list):
    if len(numbers_list) == 1:
        return numbers_list[0]
    else:
        return max(numbers_list[0],largest_number(numbers_list[1:]))
    
    print(largestnumber([1,4,3,5]))